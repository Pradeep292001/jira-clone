// Removed - no longer needed with Firebase
// Mock data is now replaced with Firebase Firestore real-time database
// Initial demo data can be added manually through the app UI

export const initializeMockData = () => {
    console.log('Using Firebase - no mock data initialization needed');
    // Demo users should be created through the registration flow
    // Or you can create them manually in Firebase Console
};
